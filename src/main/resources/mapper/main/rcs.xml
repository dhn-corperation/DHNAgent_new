<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dhn.client.rcs.mapper.SendRequest">

    <select id="req_rcs_count" parameterType = "com.dhn.client.bean.SQLParameter" resultType = "int">
        <choose>
            <when test="database == 'oracle'">
                SELECT count(1) AS cnt
                FROM ${msg_table}
                WHERE STATUS = '0'
                    AND RESERVE_DT &lt;= SYSDATE
                    AND MESSAGE_TYPE LIKE #{msg_type}
                    AND SEND_GROUP IS NULL
            </when>
            <when test="database == 'mysql'">
                SELECT count(1) AS cnt
                FROM ${msg_table}
                WHERE STATUS = '0'
                    AND RESERVE_DT &lt;= NOW()
                    AND MESSAGE_TYPE LIKE #{msg_type}
                    AND SEND_GROUP IS NULL
            </when>
        </choose>
    </select>

    <update id="req_rcs_group_update" parameterType = "com.dhn.client.bean.SQLParameter" >
        <choose>
            <when test="database == 'oracle'">
                UPDATE ${msg_table}
                SET STATUS = '1',
                    SEND_GROUP = #{group_no}
                WHERE STATUS = '0'
                    AND RESERVE_DT &lt;= SYSDATE
                    AND MESSAGE_TYPE LIKE #{msg_type}
                    AND SEND_GROUP IS NULL
                    AND ROWNUM &lt;= 200
            </when>
            <when test="database == 'mysql'">
                UPDATE ${msg_table}
                SET STATUS = '1',
                    SEND_GROUP = #{group_no}
                WHERE STATUS = '0'
                    AND RESERVE_DT &lt;= NOW()
                    AND MESSAGE_TYPE LIKE #{msg_type}
                    AND SEND_GROUP IS NULL
                LIMIT 200
            </when>
        </choose>
    </update>

    <select id="req_rcs_select" parameterType = "com.dhn.client.bean.SQLParameter" resultType="com.dhn.client.bean.RCSRequestBean">
        <choose>
            <when test="database == 'oracle'">
                SELECT
                    MSG_ID AS msgid,
                    MESSAGE_TYPE AS messagetype,
                    MESSAGE AS msg,
                    MSG AS msgsms,
                    PHN AS phn,
                    P_INVOICE as pinvoice,
                    SMS_SENDER AS smssender,
                    SMSLMS_TIT AS smslmstit,
                    SMS_KIND as smskind,
                    KISA_CODE as kisacode,
                    TO_CHAR(SYSDATE, 'YYYY-MM-DD HH24:MI:SS') AS regdt,
                    '00000000000000' AS reservedt,
                    RCS_BASE_ID AS rcsbaseid,
                    RCS_BUTTON AS rcsbutton,
                    RCS_AGENCY_ID AS rcsagencyid,
                    RCS_AGENCY_KEY AS rcsagencykey,
                    RCS_BRANDKEY AS rcsbrandkey
                FROM ${msg_table}
                WHERE SEND_GROUP = #{group_no}
            </when>
            <when test="database == 'mysql'">
                SELECT
                MSG_ID AS msgid,
                MESSAGE_TYPE AS messagetype,
                MESSAGE AS msg,
                MSG AS msgsms,
                PHN AS phn,
                P_INVOICE as pinvoice,
                SMS_SENDER AS smssender,
                SMSLMS_TIT AS smslmstit,
                SMS_KIND as smskind,
                KISA_CODE as kisacode,
                DATE_FORMAT(NOW(), '%Y-%m-%d %H:%i:%s') AS regdt,
                '00000000000000' AS reservedt,
                RCS_BASE_ID AS rcsbaseid,
                RCS_BUTTON AS rcsbutton,
                RCS_AGENCY_ID AS rcsagencyid,
                RCS_AGENCY_KEY AS rcsagencykey,
                RCS_BRANDKEY AS rcsbrandkey
                FROM ${msg_table}
                WHERE SEND_GROUP = #{group_no}
            </when>
        </choose>
    </select>

    <update id="req_rcs_sent_complete" parameterType = "com.dhn.client.bean.SQLParameter" >
        <choose>
            <when test="database == 'oracle'">
                UPDATE ${msg_table}
                SET STATUS = '2',
                    RESPONSE_DT = SYSDATE
                WHERE SEND_GROUP = #{group_no}
            </when>
            <when test="database == 'mysql'">
                UPDATE ${msg_table}
                SET STATUS = '2',
                    RESPONSE_DT = NOW()
                WHERE SEND_GROUP = #{group_no}
            </when>
        </choose>
    </update>

    <update id="req_rcs_sent_init" parameterType = "com.dhn.client.bean.SQLParameter" >
        <choose>
            <when test="database == 'oracle'">
                UPDATE ${msg_table}
                SET STATUS = '0',
                    SEND_GROUP = NULL,
                    RESPONSE_DT = NULL
                WHERE SEND_GROUP = #{group_no}
            </when>
            <when test="database == 'mysql'">
                UPDATE ${msg_table}
                SET STATUS = '0',
                    SEND_GROUP = NULL,
                    RESPONSE_DT = NULL
                WHERE SEND_GROUP = #{group_no}
            </when>
        </choose>
    </update>

</mapper>
